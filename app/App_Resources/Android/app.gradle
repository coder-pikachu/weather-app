// Add your native dependencies here:

android {
  defaultConfig {  
    generatedDensities = []
    applicationId = "org.hightimeshq.weatherapp"
  }
  aaptOptions {  
    additionalParameters "--no-version-vectors"  
  }  
} 
